# Nginx vhost for HealNest (adjust paths, PHP-FPM socket, and server_name)
server {
    listen 80;
    server_name example.com; # <-- replace with your domain
    root /var/www/healnest/current/public;
    index index.php index.html;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;
        fastcgi_pass unix:/run/php/php8.1-fpm.sock; # adjust PHP-FPM socket
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
        internal;
    }

    location ~ /\.ht {
        deny all;
    }

    # Allow ACME challenge for certbot
    location ^~ /.well-known/acme-challenge/ {
        allow all;
    }
}

# SSL block example (after obtaining certs with certbot)
# server {
#     listen 443 ssl http2;
#     server_name example.com;
#     root /var/www/healnest/current/public;
#     ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;
#     include /etc/letsencrypt/options-ssl-nginx.conf;
#     ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;
#     # copy rest of the config from above
# }
