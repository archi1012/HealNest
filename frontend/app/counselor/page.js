'use client';
import AppShell from '../../components/app-shell'; import { useApi } from '../../components/use-api';
export default function CounselorPage() { const directory = useApi('/counselor/directory'); const alerts = useApi('/counselor'); return <AppShell title="Counselor Support"><section className="rounded-xl bg-white p-5 shadow"><h2 className="font-heading text-xl">Available counselors</h2>{directory.data?.counselors.map((counselor) => <p key={counselor.id} className="mt-3 text-sm"><b>{counselor.name}</b> — {counselor.email}</p>)}</section>{alerts.data && <section className="mt-6 rounded-xl bg-white p-5 shadow"><h2 className="font-heading text-xl">Open alerts</h2>{alerts.data.alerts.map((alert) => <p key={alert.id} className="mt-2 text-sm">{alert.user_name}: {alert.risk_level} risk</p>)}</section>}</AppShell>; }
