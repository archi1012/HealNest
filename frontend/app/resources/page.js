'use client';
import AppShell from '../../components/app-shell'; import { useApi } from '../../components/use-api';
export default function ResourcesPage() { const { data, error, loading } = useApi('/resources'); return <AppShell title="Resources & Support">{loading ? 'Loading…' : error ? error : <div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{data.resources.map((resource) => <article key={resource.id} className="rounded-xl bg-white p-5 shadow"><div className="text-3xl">{resource.icon}</div><p className="mt-3 text-xs font-semibold text-midgreen">{resource.category}</p><h2 className="font-heading text-xl">{resource.title}</h2><p className="mt-2 text-sm text-gray-600">{resource.description}</p>{resource.external_url && <a className="mt-3 inline-block text-sm text-midgreen underline" href={resource.external_url}>Open resource</a>}</article>)}</div>}</AppShell>; }
