'use client';
import { Suspense, useState } from 'react'; import { useSearchParams, useRouter } from 'next/navigation'; import { api } from '../../lib/api';
function ResetPasswordForm() { const params = useSearchParams(); const router = useRouter(); const [message, setMessage] = useState(''); async function submit(event) { event.preventDefault(); const form = new FormData(event.currentTarget); try { const result = await api('/auth/reset-password', { method: 'POST', body: JSON.stringify({ email: form.get('email'), token: params.get('token'), password: form.get('password'), passwordConfirmation: form.get('passwordConfirmation') }) }); setMessage(result.message); router.push('/login'); } catch (error) { setMessage(error.message); } } return <form onSubmit={submit} className="w-full max-w-md space-y-4 rounded-xl bg-white p-8 shadow"><h1 className="font-heading text-3xl">Choose a new password</h1><input name="email" defaultValue={params.get('email') || ''} type="email" required className="w-full rounded border p-3"/><input name="password" type="password" minLength="8" required placeholder="New password" className="w-full rounded border p-3"/><input name="passwordConfirmation" type="password" minLength="8" required placeholder="Confirm password" className="w-full rounded border p-3"/><button className="rounded bg-forest px-4 py-2 text-white">Reset password</button>{message && <p className="text-sm">{message}</p>}</form>; }
export default function ResetPasswordPage() { return <main className="grid min-h-screen place-items-center bg-cream p-6"><Suspense fallback="Loading…"><ResetPasswordForm /></Suspense></main>; }
