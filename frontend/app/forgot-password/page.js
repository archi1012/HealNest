'use client';
import Link from 'next/link'; import { useState } from 'react'; import { api } from '../../lib/api';
export default function ForgotPasswordPage() { const [message, setMessage] = useState(''); async function submit(event) { event.preventDefault(); try { const result = await api('/auth/forgot-password', { method: 'POST', body: JSON.stringify({ email: new FormData(event.currentTarget).get('email') }) }); setMessage(result.message); } catch (error) { setMessage(error.message); } } return <main className="grid min-h-screen place-items-center bg-cream p-6"><form onSubmit={submit} className="w-full max-w-md space-y-4 rounded-xl bg-white p-8 shadow"><h1 className="font-heading text-3xl">Reset password</h1><input name="email" type="email" required placeholder="Email" className="w-full rounded border p-3"/><button className="rounded bg-forest px-4 py-2 text-white">Send reset link</button>{message && <p className="text-sm">{message}</p>}<Link href="/login" className="block text-sm text-midgreen underline">Back to sign in</Link></form></main>; }
