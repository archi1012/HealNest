import { Router } from 'express'; import { counselorDashboard, counselorDirectory, counselorUserDetail, createCounselorNote, resolveAlert } from '../controllers/counselor.controller.js'; import { requireAuth, requireRole } from '../middleware/auth.middleware.js'; const router = Router(); router.get('/directory', requireAuth, counselorDirectory); router.use(requireAuth, requireRole('counselor', 'admin')); router.get('/', counselorDashboard); router.get('/users/:userId', counselorUserDetail); router.post('/users/:userId/notes', createCounselorNote); router.patch('/alerts/:id/resolve', resolveAlert); export default router;
