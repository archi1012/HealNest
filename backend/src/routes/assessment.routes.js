import { Router } from 'express'; import { assessmentIndex, assessmentQuestions, assessmentResult, submitAssessment } from '../controllers/assessment.controller.js'; import { requireAuth } from '../middleware/auth.middleware.js'; const router = Router(); router.use(requireAuth); router.get('/', assessmentIndex); router.get('/result/:id', assessmentResult); router.get('/:type/questions', assessmentQuestions); router.post('/:type', submitAssessment); export default router;
