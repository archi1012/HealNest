import { Router } from 'express'; import { appointments, createAppointment, manageAppointments, updateAppointmentStatus } from '../controllers/appointment.controller.js'; import { requireAuth, requireRole } from '../middleware/auth.middleware.js'; const router = Router(); router.use(requireAuth); router.get('/', appointments); router.post('/', createAppointment); router.get('/manage', requireRole('counselor', 'admin'), manageAppointments); router.patch('/:id/status', requireRole('counselor', 'admin'), updateAppointmentStatus); export default router;
